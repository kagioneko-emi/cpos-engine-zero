FROM python:3.11-slim

WORKDIR /app

# Install basic tools and project dependencies
RUN pip install --no-cache-dir ruff pytest requests flask pyyaml cryptography

# Create a non-root user
RUN useradd -m sandboxuser
USER sandboxuser

CMD ["bash"]
